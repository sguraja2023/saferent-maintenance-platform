# SafeRent Frontend

This is the SafeRent React frontend. It uses Vite, React, TypeScript, and React Router.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

## Current Step

Step 0.3 adds a simple routed home page at `/`. The frontend does not call the backend yet; that connection is the next slice.
